- games.each_with_index do |game, i|
  %div{id: "match#{game.slot}", class: "match m#{i+1}"}
    %p.slot.slot1
      %span.seed= game.team_one.try(:seed)
      = game.team_one.try(:name)
    %p.slot.slot2
      %span.seed= game.team_two.try(:seed)
      = game.team_two.try(:name)
