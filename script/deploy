#/bin/bash

bundle exec rake assets:precompile
git add .
git ci -m"precompile assets for heroku"
git push staging unicorn:master #push local unicorn branch to staging/master
bundle exec rake figaro:heroku --remote staging
heroku run rake db:migrate --remote staging

git rm -r public/assets
git ci -m"remove precomiled assets post deploy"
